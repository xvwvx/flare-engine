// <自动生成> 对应 C++ 源文件：resource.h
// 注意：System, System.Collections.Generic, System.Linq, System.Threading.Tasks 已全局导入，此处省略。

namespace FlareEngine
{
    /// <summary>
    /// 对应 C++ 的 Windows 资源脚本 (.rc) 常量定义文件 resource.h。
    /// IDI_FLARE_APP_ICON 是 Win32 资源编译器用来引用可执行文件图标资源的整数 ID，
    /// 仅在 Windows 平台的 .rc 资源脚本编译期使用，不参与任何运行时业务逻辑。
    /// 在 .NET 中，应用程序图标改由项目文件 (.csproj) 的 &lt;ApplicationIcon&gt; 属性配置，
    /// 不存在与之等价的运行时常量，因此该值本身没有运行时逻辑迁移需求；
    /// 为保留常量的可追溯性，仍以常量形式声明。
    /// </summary>
    public static class ResourceIds
    {
        public const int IdiFlareAppIcon = 101;
    }
}
